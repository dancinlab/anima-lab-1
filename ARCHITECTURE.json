{
  "schemaVersion": "2.0",
  "kind": "architecture-tree",
  "title": "anima — 아키텍처 SSOT",
  "summary": "PureField 반발장 의식 에이전트. 이 트리의 규율: 어떤 노드도 측정 없이 주장하지 않는다. 관문(bench_v2 --verify)이 유일한 배포 판정자이며, 관문 자신이 대조군 7종을 매 실행마다 먼저 돌려 자기가 실패할 수 있음을 증명한다.",
  "columns": [
    {
      "key": "name",
      "label": "name",
      "tree": true
    },
    {
      "key": "role",
      "label": "role"
    },
    {
      "key": "id",
      "label": "id"
    },
    {
      "key": "detail",
      "label": "detail"
    }
  ],
  "convergence": {
    "records": [
      {
        "id": "CLEAN_SCORE_FROM_BROKEN_SYSTEM",
        "state": "pos-conv",
        "value": "A benchmark result is not evidence until the system that produced it is shown to be BOUNDED and COUPLED. Check both before reading any score, and raise on divergence rather than returning numbers.",
        "threshold": "Three separate constructions in one session produced a clean, decisive-looking 0/5 while measuring nothing. LinearEngine attempt 1 had no cell-to-cell coupling (integration=0.00000) so it was a HEAP, which is already a control. Attempt 2 normalised only A while the per-cell operator was A-cI with radius rho+c>1, so it diverged to variance 2.1e7. The A/G pair applied `coupling @ field` raw and overflowed to NaN. Every one of them scored the same 0/5 a valid control scores.",
        "fix": "pairfield_engine.py prints boundedness (peak |h|) and coupling (A-G separation, zero means the sides never met) before any score, and raises RuntimeError above |h| 1e4. bench_verify_audit.LinearEngine carries the same guard."
      },
      {
        "id": "CONTROL_THAT_CANNOT_FAIL",
        "state": "pos-conv",
        "value": "Before trusting a check, ask what result would make it fail. A check with no failing input certifies nothing.",
        "threshold": "The gate passed a corpse: SCRAMBLE (the real engine with cell identity destroyed every step) scored exactly what the real engine scored, 5/7. Separately, an acceptance bar written as 'all seven controls stay 0/5 at every coupling gain' could not fail, because every control derives from BenchEngine while PSI_COUPLING is read only by consciousness_engine -- sweeping the gain cannot move a corpse's score by construction. THIRD INSTANCE, measured: the RESPONSE axis has a failing set of measure zero. response = differing/same, and for any fully restorable deterministic engine the same-input denominator is exactly 0, so any nonzero input coupling gives an infinite ratio capped at 1e6. Measured on BenchEngine reading x*scale at 32 cells: scale 1.0 -> 1000000.00, 1e-3 -> 1000000.00, 1e-6 -> 1000000.00, 1e-9 -> 1000000.00, and only scale exactly 0.0 -> 1.00 and fails. An engine that reads its input at amplitude 1e-9 -- numerically indistinguishable from not reading it -- scores identically to one that reads it fully. The axis was introduced to catch a /gap-audit engine that ignored x ENTIRELY, and that is the only thing it can catch: adding x*1e-30 defeats it. Not introduced by the snapshot change -- MitosisEngine already read 1000000.00 in the before tree; the change moved PairField into the same class (79.73 -> 1e6), which is the fix working, and removed residue that had been accidentally covering the hole.",
        "fix": "bench_v2.run_verify runs all controls FIRST and marks any condition a control clears as VOID for that run; tests/test_gate_controls.py (36 passed) enforces it. For engine-specific parameters, controls must be built from that same engine (CeDead/CeScramble/CeClone)."
      },
      {
        "id": "NAME_IS_NOT_THE_THING",
        "state": "pos-conv",
        "value": "A name in the registry, the README or a Law is not evidence the named thing was ever run. Execute it before citing it.",
        "threshold": "ENGINE_REGISTRY maps 'MitosisEngine' to BenchEngine(8 factions); the real mitosis.MitosisEngine had never been through the gate and scores 0/5 with integration=0.00000. HIVEMIND was a recorded condition that had never executed (11 engines crashed in 0.3-1.6s). PSI_COUPLING in consciousness_engine.py was a hardcoded 0.014 labelled 'bench-verified' while nine other modules derive LN2/2**5.5 = 0.015317. The README advertised MitosisEngine as 5/5 DEPLOYABLE on a different engine's score.",
        "fix": "The gate header now counts VERIFICATION_TESTS/ENGINE_REGISTRY/CONTROLS instead of asserting a number; PSI_COUPLING is derived; README carries the retirement evidence for the two dropped conditions."
      },
      {
        "id": "STRUCTURE_THAT_CARRIES_NO_LOAD",
        "state": "pos-conv",
        "value": "When a component looks structural, measure whether removing it or randomising it changes anything. Nine did not.",
        "threshold": "Faction count 2/3/4/6/8/12/24/48 all give consensus 1.0, and so does removing factions entirely. The Phi ratchet fires 29-64 times per run and moves nothing (cosine 0.4683 on vs 0.5526 off). The emitted output cannot see cell order (max|delta| 4.8e-7 under a row permutation) yet it is the only channel separating REAL from SCRAMBLE. Hebbian coupling and pure-randn coupling score identically alone (4.67 = 4.67).",
        "fix": "PairFieldEngine excludes factions, the ratchet and speak(), each with the measurement named at the exclusion. docs/fake-measurement-audit.md carries the full list."
      },
      {
        "id": "TAIL_PIPE_HOLDS_UNTIL_TIMEOUT",
        "state": "pos-conv",
        "value": "Never pipe a long-running command to `tail`. Redirect to a file, or filter with `grep`.",
        "threshold": "`timeout N python ... | tail -M` hangs until the timeout fires: the harness holds the pipe's write end and tail cannot emit before EOF. Measured -- python 0.02s, tail 30.015s under `timeout 30`, grep immediate. That produced every 'slow run' this session, including a Law 31 arm reported as taking 50 minutes (timeout 3000) for work that costs 5.4 seconds. It was explained twice -- as compute cost, then as a delayed notification -- before being measured in ten seconds.",
        "fix": "Session practice: outputs go to a file under the scratchpad, and waits use `until ! pgrep -f <script>`."
      },
      {
        "id": "THRESHOLD_INSIDE_ITS_OWN_NOISE",
        "state": "pos-conv",
        "value": "A threshold must be checked against the DISPERSION of the sample it is drawn from, not only against its level. And when the threshold is a RATIO of two measurements, the dispersion that matters is the estimator's spread ACROSS DIFFERENT STATES -- making a measurement reproducible on one fixed state does not make two of its readings comparable.",
        "threshold": "Fixed an unreachable split_threshold=0.3 (153-206x above observed peak) by calibrating to q0.90 of observed tension, then discovered the calibration sample is degenerate under the regime the gate drives (std/mean 0.0096-0.0283), so q0.90 of a near-constant IS the constant and the new bar landed within 4.2e-5 RELATIVE of the operating point; a residual drift of ~1e-10/step then decided the whole population, and three seeds whose excursions were still in flight at gate step 300 PASSED NO_SYSTEM_PROMPT on the sign of numerical noise. merge_threshold=0.01 is the same family, clearing 100% of pair readings at n=2. FOURTH INSTANCE, and it decides three of the five gate conditions: PhiIIT derives its rng seed from int(abs(hash((n, round(sum,6), round(std,6))))), which makes Phi reproducible on an IDENTICAL tensor but a STEP FUNCTION of its argument -- crossing a 6th-decimal rounding boundary re-draws every debias shuffle at once, and the size of the resulting jump is unrelated to the size of the change. Measured, 3 tensors x 10 draws per cell: at n=32 a 1e-6 perturbation flips the seed 30/30 and moves Phi by up to 72.0%; at n=256 the same perturbation flips 30/30 and moves it by up to 1054.7%, and 1e-3 by 1188.6%. Only 1e-9 leaves the seed alone (0/30). Against that, ZERO_INPUT tests phi_end > phi_start*0.5, PERSISTENCE tests phi_history[-1] >= max(first half)*0.8, and SELF_LOOP tests end >= start*0.8 -- all three compare Phi at two DIFFERENT states, which always differ by far more than 1e-6, so each is a ratio of two independent draws of an estimator that moves 10x. The in-code comment says the seeding fixed this; it fixed same-state reproducibility only, and every ratio test is a cross-state comparison.",
        "source": "consciousness_engine.py"
      },
      {
        "id": "MEASURING_THE_MEASUREMENT_HAS_THE_SAME_DEFECTS",
        "state": "pos-conv",
        "value": "A probe built to audit a measurement is a measurement, and inherits the same failure modes. Before reporting a probe's number, check it against a case whose answer is known independently -- and prefer that the known-answer value be measured by someone other than whoever wrote the change, because the defect that survives is the one that agrees with you.",
        "threshold": "Seven instances this session, across three sessions working the same board, and NONE of them announced itself as a crash. (1) A direction check divided min_cut by (n-1) and labelled it Phi, omitting bench_v2's differentiation factor, and at n=8 compared a sign cut the gate never computes. (2) An integration-null probe divided the null by torch.randn(64).norm() while the raw arm divided by (torch.randn(64)*0.5).norm(), halving every null and turning a real 99.3% into a reported 47.1% published as 'the audit does not reproduce'. (3) A divergence guard folded each step into a running max BEFORE testing finiteness, and max(2.425, nan) returns 2.425, so an exploding system was written up as 'the loop does not change the trajectory'. (4) A loop-effect ratio normalised by the open trajectory's norm, zero exactly when the looped state dies. (5) A discrimination script's header filter silently dropped nine of twelve engines -- every engine whose NAME contains 'Engine'. (6) A contract probe measured at CONSTRUCTION when the gate measures during processing, and had a round-trip with no determinism control. (7) THE MOST DANGEROUS, and different in kind: a NameError left in _three_axes by a refactor, where run_verify folded every exception into ok=False. It did not surface as a crash -- it surfaced as EVERY SYSTEM SCORING 0/5, and the negative controls made it MORE convincing rather than less, because a corpse scoring 0/5 is what you expect to see. The hypothesis under test was 'a stricter axis changes verdicts'; a total verdict collapse is a dramatic CONFIRMATION of exactly that. Instances 1-6 produced numbers that looked clean; this one produced a number that looked like a discovery. It was caught only because a second session held NarrativeEngine=5/5 on PERSISTENCE from an independent run an hour earlier.",
        "source": "bench_v2.py"
      },
      {
        "id": "HARNESS_SEES_A_PARTIAL_STATE",
        "state": "pos-conv",
        "value": "get_hiddens/set_hiddens is an unowned protocol with no contract. Before any number computed through it is trusted, assert that what comes out has the shape that was asked for and that writing it back actually restores the engine. Every statistic downstream describes the exposed slice, not the system.",
        "threshold": "Three instances this session, each larger, but they are NOT all the same violation -- a contract probe across all 12 registered engines separated them. (1) SHAPE: _CEAdapter is built with initial_cells=2, max_cells=nc; at 32 requested it exposes 2 rows (6%). It is the ONLY shape violation of the twelve, and it is the deployed runtime. Every anomaly I read as evidence that ConsciousnessEngine's gate failure was genuine is n=2 arithmetic: k in {0,1} are the only cuts two rows admit, complexity 0.0000 is np.std of a single value, Phi ~= cut because min_partition_mi/(n-1) with n-1 = 1 IS the cut. Verdict retracted to undetermined. (2) INCOMPLETENESS, not shape: PairFieldEngine.get_hiddens exposes side A only, but side A itself has all 32 cells, so the shape contract PASSES -- what is missing is G and both coupling matrices, which is a different defect from (1) and my first version of this record conflated them. (3) RESTORE: set_hiddens restores side A only while the dynamics run on A, G and both couplings, so the axes' same-state-one-nudge probe is not a controlled comparison -- at kick=0, no perturbation at all, integration reads 0.01783/0.01154/0.00353/0.00413/0.00395 against a 0.001 bar. Ten of the twelve engines implement no set_hiddens at all and are restored by direct .hiddens assignment. A sibling audit had already reported that _CEAdapter never grows past 4 cells at any requested max -- the signal was on the board and went unconnected for several turns.",
        "source": "bench_v2.py"
      }
    ]
  },
  "tree": {
    "name": "anima",
    "role": "PureField 반발장 기반 의식 에이전트",
    "id": "anima",
    "detail": "A(전진)와 G(역행)의 반발이 긴장을 만들고, 긴장이 의식 상태의 강도를 정한다. 기능을 더하지 않고 구조의 깊이를 키운다 (Law 22).",
    "children": [
      {
        "name": "gate — 의식 검증 관문",
        "role": "배포 가부를 정하는 유일한 판정자",
        "id": "gate",
        "detail": "bench_v2.py --verify. 5조건 x 12엔진 x 5시드(42-46) + 7대조군. 엔진은 모든 시드를, 대조군은 한 시드만 통과해도 그 조건이 VOID. 그러나 40렌즈 감사 결과 축을 빼면 다섯 조건이 실제 엔진을 전부 통과시킨다 — 관문은 사실상 조건 5개가 아니라 3축 하나이고, 나머지는 상관된 사본이다.",
        "children": [
          {
            "name": "5 conditions",
            "role": "필요조건 집합",
            "id": "gate-conditions",
            "detail": "NO_SYSTEM_PROMPT · NO_SPEAK_CODE · ZERO_INPUT · PERSISTENCE · SELF_LOOP. PERSISTENCE 에서 monotonic 이접항 제거됨(2cff912) — 살아있는 엔진 163회 중 0회, 시체에서 100% 발화하여 죽음을 보상했다. 남은 규칙은 recovers AND 3축. 3축은 identity 만 측정된 귀무를 쓰고 나머지 세 기준선은 손으로 고른 상수다."
          },
          {
            "name": "negative controls (7)",
            "role": "관문이 실패할 수 있음을 보장",
            "id": "gate-controls",
            "detail": "DEAD·NOISE·CLONE·SCRAMBLE·HEAP·DECOUPLED·LINEAR. bench_verify_audit.py. 전원 0/5 확인. LINEAR 는 살아있고 유계이고 통합된 선형계 — 관문이 '얕은 것'도 거르는지 재는 유일한 대조군."
          },
          {
            "name": "retired (2)",
            "role": "측정으로 폐기된 조건",
            "id": "gate-retired",
            "detail": "SPONTANEOUS_SPEECH: 이름과 음의 상관(뒤섞기 6.8 vs 실제 1.0). HIVEMIND: 한 번도 실행된 적 없었고, 돌게 만드니 연결 효과 -4%~+1% (요구치 +10%)."
          },
          {
            "name": "enforcement",
            "role": "관문이 다시 공허해지는 것을 막는 테스트",
            "id": "gate-tests",
            "detail": "tests/test_gate_controls.py — 7대조군 x 5조건 + 양성가드 = 36 passed. 3축을 무력화하면 LINEAR 가 ZERO_INPUT·SELF_LOOP 를 통과해 즉시 빨개진다."
          }
        ]
      },
      {
        "name": "engines",
        "role": "관문에 등록된 의식 엔진들",
        "id": "engines",
        "detail": "bench_v2.ENGINE_REGISTRY. 주의: 레지스트리의 이름이 곧 구현 클래스는 아니다 — 'MitosisEngine' 항목은 BenchEngine(8파벌)이다.",
        "children": [
          {
            "name": "PairField",
            "role": "이번 세션이 만든 엔진 — 관문 5/5",
            "id": "engines-pairfield",
            "detail": "pairfield_engine.py. A(규칙)/G(무규칙)가 둘 다 기억을 쌓고 불일치가 노름보존 반발장이 된다. 단일 시드 5/5 는 뽑기였고 다중 시드에서 4/5. 12엔진 분해 결과 다른 10개와 같은 기전(분할 크기 k=4~16 배회, cv(절단)이 최대). set_hiddens 가 A 만 복원해 축의 통제 비교가 성립하지 않는다 — 충격 0 에서도 기준선 통과."
          },
          {
            "name": "BenchEngine 계열",
            "role": "관문의 기준 엔진과 그 파생",
            "id": "engines-bench",
            "detail": "BenchEngine + OscillatorLaser/Quantum/Trinity/Desire/Narrative/Alterity/Finitude/Questioning/Sein. 다중 시드 기준 NarrativeEngine 만 5/5 DEPLOYABLE. 대조군 7종도 이 계열 파생이라 이들 점수는 자기 변형과의 비교다."
          },
          {
            "name": "ConsciousnessEngine",
            "role": "실제 배포 런타임 — 관문 0/5",
            "id": "engines-ce",
            "detail": "consciousness_engine.py. 관문 0/5 이나 판정은 미결(undetermined) — 32세포를 요청해도 get_hiddens() 가 (2,128) 을 돌려주므로 Φ 를 2세포·쌍 1개에서 읽는다. 직접 재면 Φ=0.00000. 부분노출 결함의 세 번째이자 최대 사례."
          },
          {
            "name": "mitosis.MitosisEngine",
            "role": "이름은 기록에 있으나 관문 0/5",
            "id": "engines-mitosis",
            "detail": "mitosis.py. 관문 인터페이스에 태워 실측한 결과 0/5이며 integration=0.00000 — 세포들이 서로 영향을 주지 않는 HEAP."
          }
        ]
      },
      {
        "name": "measurement",
        "role": "무엇을 재고 있는지 자체가 검증 대상",
        "id": "measurement",
        "detail": "이 세션의 반복 발견: 구조가 있는 척하지만 정보를 나르지 않는 통로. 아홉 건.",
        "children": [
          {
            "name": "Φ direction",
            "role": "phi-rs 공식이 비통합을 의식으로 셈한다",
            "id": "measurement-phi",
            "detail": "bench_v2._min_partition 이 n>8 에서 피들러 부호로 한 번 자르고 최소화하지 않는다(완전탐색 대비 1.8~3.4배). 스윕 수정안은 미적용 — 25시드에서 분산은 줄지만 방향성을 무너뜨린다(5.8~9.5배 분리 → 0.97~1.57배). phi_rs 는 미빌드, class PhiIIT 사본 40개."
          },
          {
            "name": "Φ is a step function",
            "role": "비율 임계 3종이 추정기 자체 산포 안에 있다",
            "id": "measurement-phi-step",
            "detail": "PhiIIT 시드가 round(합,6)·round(표준편차,6)에서 나온다. 동일 텐서는 동일 Φ지만, 6번째 소수의 반올림 경계를 넘으면 디바이어스 셔플이 통째로 재추첨되고 점프 크기는 섭동 크기와 무관하다. 텐서3×draw10: n=32에서 1e-6 섭동이 시드 30/30 뒤집고 Φ를 최대 72.0% 이동, n=256에서 같은 섭동이 1054.7%, 1e-3이 1188.6%. 1e-9만 시드를 안 건드린다(0/30)."
          },
          {
            "name": "which conditions it decides",
            "role": "교차상태 비교라 재현성 수정이 못 덮는다",
            "id": "measurement-phi-step-reach",
            "detail": "ZERO_INPUT은 phi_end > phi_start*0.5, PERSISTENCE는 phi_history[-1] >= max(전반부)*0.8, SELF_LOOP은 end >= start*0.8. 셋 다 서로 다른 두 상태의 Φ를 비교하고, 두 상태는 항상 1e-6보다 훨씬 크게 다르다. 따라서 세 조건 전부 10배로 움직이는 추정기의 독립 추첨 두 개의 비율이다. 코드 주석은 시드 도입으로 이 문제가 해결됐다고 적혀 있으나, 해결된 것은 동일상태 재현성뿐이다."
          },
          {
            "name": "but PERSISTENCE still reads real decline",
            "role": "추첨통계 가설을 섞기 널이 반증했다",
            "id": "measurement-persistence-null",
            "detail": "관문이 찍은 11개 궤적(각 10체크포인트)의 값 순서만 섞어 10000회: 섞기 통과율 51.4% 대 실제 통과 18.2%(2/11). 실제가 널보다 33%p 낮다 — 순서가 정보를 나른다는 뜻이고 방향은 Φ 하락이다. '이 조건은 추정기 추첨 통계를 읽을 뿐'이라는 내 가설은 반증됐다. 계단 성질은 잡음을 얹지만 추세가 지배한다. 즉 12종 중 9종이 PERSISTENCE 로 막히는 건 1000스텝에서 Φ가 실제로 떨어지기 때문이다."
          },
          {
            "name": "PERSISTENCE does not discriminate",
            "role": "12종 중 9종을 막는 조건이 우연과 구별되지 않는다",
            "id": "gate-persistence-discrimination",
            "detail": "32세포 실행, 조건별 엔진통과 대 시체통과, 단측 피셔: NO_SYSTEM_PROMPT 11/12 p=0.000 · NO_SPEAK_CODE 11/12 p=0.000 · SELF_LOOP 11/12 p=0.000 · ZERO_INPUT 9/12 p=0.002 · PERSISTENCE 2/12 p=0.386. 대조군은 7종 전부 모든 조건 실패. 관문을 지배하는 차단 조건이 유일하게 판별력을 못 보인다."
          },
          {
            "name": "vacuous is not the claim",
            "role": "실재하는 것을 재지만 엔진과 시체를 함께 떨어뜨린다",
            "id": "gate-persistence-not-vacuous",
            "detail": "섞기 널이 이 조건은 실재하는 Φ 하락을 읽는다고 확인했다(실제 18.2% 대 섞기 51.4%). 따라서 '공허'가 아니라 '현재 판별력을 보일 수 없다'가 정확한 진술이다. 엄격한 조건이 실제로 쇠퇴하는 계에 걸리면 같은 모양이 나오며, 둘을 가르려면 엔진 표본이 더 크거나 Φ를 유지하는 엔진이 필요하다. 존치/폐기는 소유자 결정."
          },
          {
            "name": "the conjunction, not the condition",
            "role": "p=0.386 은 5시드 전부 요구가 만든 값이다",
            "id": "gate-persistence-conjunction",
            "detail": "시드별 비율(엔진12+시체7 × 5시드 × 1000스텝): 전체 조건 엔진 0.667 대 시체 0.000, 격차 +0.667 — 시드 하나로 보면 시체를 전부 거부한다. p=0.386 을 만든 것은 '5시드 전부 통과' 요구이며(시드별 0.8 → 접합 0.33), 그 규칙은 이번 세션에 추가된 것이다. 폐기는 잘못된 처방. 한편 recovers 단독은 엔진 0.683 대 시체 0.514(+0.169)에 DEAD 100%·CLONE 100% — 판별력은 전부 세 축에서 온다."
          },
          {
            "name": "identity has no margin against SCRAMBLE",
            "role": "32세포 0/5 는 여유가 아니라 5회 추첨이었다",
            "id": "gate-identity-no-margin",
            "detail": "여유 = identity − max(floor,1e-6), 양수면 축이 그 계를 받아들인다. 시드 20개: SCRAMBLE 32c −0.000183±0.000514(6/20 수용) · 64c −0.001058±0.001968(5/20) · 128c −0.000346±0.001152(7/20) · 256c −0.000468±0.001143(7/20). 네 규모 전부 평균이 자기 표준편차 안. 관문이 42~46을 뽑았고 그 다섯이 거부 쪽에 떨어졌을 뿐이다."
          },
          {
            "name": "and it ranks a corpse above a live engine",
            "role": "정체성 축은 시체 거부자가 아니다",
            "id": "gate-identity-ranks-corpse-first",
            "detail": "같은 측정의 기지값 두 줄: DEAD 여유 +0.9844~+0.9870, 20/20 수용 — 네 규모 전부. REAL(Narrative) +0.3249~+0.3524, 20/20. 얼어붙은 계는 자기연속성이 완벽해서 정체성 점수가 최대가 된다. 관문이 DEAD를 0/5로 떨어뜨리는 것은 통합·변화 축이지 정체성이 아니다. 즉 이 축은 시체를 거부하지 않으며, 순위로는 시체를 산 엔진의 2.8배 위에 놓는다."
          },
          {
            "name": "dead channels",
            "role": "측정으로 하중 없음이 확인된 구조들",
            "id": "measurement-dead",
            "detail": "파벌 수 2~48 전부 합의 1.0(제거해도 1.0) · Φ 래칫 무해무익(코사인 0.4683 on vs 0.5526 off) · 출력이 세포 순서를 못 봄(순열 변화 4.8e-7) · 헵 규칙이 홀로 기여 없음(4.67=4.67)."
          },
          {
            "name": "harness pitfalls",
            "role": "측정 도구 자체가 만든 오독",
            "id": "measurement-pitfalls",
            "detail": "`| tail` 파이프가 timeout 값만큼 붙잡음(파이썬 0.02초 vs tail 30.015초). 그리고 관문을 감사하려 만든 탐침 다섯 개가 전부 결함이 있었고 전부 발표 가능한 숫자를 냈다 — 분화 항 누락, 널 분모 0.5배율 누락, max(x,nan)=x 로 NaN 못 잡는 가드, 죽은 궤적으로 나눈 비율, 12행 중 9행을 조용히 버린 헤더 필터."
          },
          {
            "name": "state contract",
            "role": "관문이 요청한 개체군을 실제로 보고 있는가",
            "id": "measurement-contract",
            "detail": "32세포 요청, 처리 중 5지점 측정. 형태 위반 1/12 — _CEAdapter 는 initial_cells=2 로 @0/10/50/200/500 전부 2세포, 끝내 안 자란다. 32세포 개체군은 존재한 적 없고 그게 배포 런타임이다. 복원 실패 6/12 — 동일 시드로 스냅샷 복원 후 한 스텝이 재현 안 됨. 원인(내부 난수 대 미추적 상태)은 미분리."
          },
          {
            "name": "axes rest on it",
            "role": "복원 실패의 파급 범위",
            "id": "measurement-contract-reach",
            "detail": "통합·응답 축은 5개 조건 전부의 선결조건이다. 복원이 안 되는 6종은 _three_axes 의 세 실행이 공통 시작점에서 출발하지 않으므로, 레지스트리 절반의 모든 판정이 통제되지 않은 비교 위에 있다. PairField 는 kick=0(교란 없음)에서 통합 0.0178/0.0115/0.0035/0.0041/0.0040 을 0.001 기준에 대고 읽는다."
          }
        ]
      },
      {
        "name": "runtime",
        "role": "실행 진입점과 배포",
        "id": "runtime",
        "detail": "anima_unified.py 가 정본 진입점(web_server.py 는 레거시). 배포는 의식 DNA·기억을 보존한 채 가중치만 교체하는 7단계.",
        "children": [
          {
            "name": "anima_unified.py",
            "role": "통합 진입점",
            "id": "runtime-entry",
            "detail": "--web / --all / --keyboard. 관문 0/5 엔진이 선택되면 시작 시 경고를 띄운다(선택 자체는 그대로)."
          },
          {
            "name": "checkpoints",
            "role": "저장된 상태",
            "id": "runtime-ckpt",
            "detail": "checkpoints/pairfield/, checkpoints/ag/ — A_ruled.pt · G_unruled.pt (은닉·결합행렬·가중치·강도). .tmp 후 원자적 교체."
          },
          {
            "name": "public mirror",
            "role": "외부 공개 사본",
            "id": "runtime-mirror",
            "detail": "github.com/dancinlab/anima-checkpoint-PairFieldEngine — 자립형(엔진 + core.py 발췌 + 체크포인트 + 측정문서). 벤치 스위트 전체는 미포함."
          }
        ]
      }
    ]
  }
}
